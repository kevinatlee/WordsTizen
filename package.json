{
  "name": "words-tizen",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny packaged Samsung TV wrapper for Words",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.js",
    "stage": "node scripts/stage.js",
    "probe": "node scripts/probe-site.js"
  },
  "engines": { "node": ">=18" }
}
